# purpose.md — 这个 Wiki 为什么存在

> 这份文件定义这个知识库的**意图**：它要回答什么、边界在哪、我当前的判断是什么。
> Claude 每次 ingest 和 query 前都先读它，确保产出服务于这里的目标。
> 占位符用 `<...>` 标出。除"演进中的判断"一节外，其余内容只在我明确要求时才改。

## 一句话定位

`<这个 wiki 是关于什么的，一句话讲清>`

例：本 wiki 追踪 Transformer 架构及其缩放性质的研究，把分散在论文里的发现综合成可查询的结构化知识。

## 目标（为什么做）

`<2–4 条，说明你希望从这个 wiki 里得到什么>`

- 目标 1：`<例：在不重读原始论文的前提下，能快速回答关于缩放规律的细致问题>`
- 目标 2：`<例：积累出一份能随新论文持续更新的领域综述>`
- 目标 3：`<...>`

## 核心问题（最想被回答的）

> 这些问题是 wiki 的北极星。Claude 在组织页面、判断该深挖什么时，以它们为准。

1. `<问题 1>`
2. `<问题 2>`
3. `<问题 3>`
4. `<问题 4（可选）>`
5. `<问题 5（可选）>`

## 范围边界

- **收进来（in scope）**：`<哪些主题、哪些类型的资料属于这个 wiki>`
- **不收（out of scope）**：`<明确排除什么，避免 wiki 发散>`
- **资料类型**：`<论文 / 文章 / 会议记录 / 个人笔记 / 数据文件 ...>`

## 受众与输出形态

- **谁来读**：`<只有我自己 / 团队 / 未来的我>`
- **页面应该长什么样**：`<例：偏综述与对比分析，少流水账；关键论断必须可回溯到原始资料>`
- **语言**：`<中文 / 英文>`

## 演进中的判断（Evolving Thesis）

> 这是全文件唯一**会随 wiki 成长而被改写**的一节，代表我（在 Claude 协助下）对这个领域当前的最佳综合判断。
> 资料积累到一定程度、或出现重要矛盾时，请 Claude 提议更新这一节，由我确认后改写。

- 当前主线判断：`<目前你对核心问题的最佳回答，一两段。一开始可以是"尚不清楚，待资料积累">`
- 尚未解决/存疑：`<还没想清楚、或资料相互矛盾的点>`
- 下一步想深挖：`<接下来最想补的方向或资料>`

## 成功标准（可选）

`<怎样算这个 wiki 做得好？例：能在 30 秒内回答核心问题且每条论断都有出处；新论文加入后综述能自动保持最新。>`
