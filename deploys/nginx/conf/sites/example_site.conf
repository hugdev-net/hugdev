server {
    listen 0.0.0.0:80;
    server_name example.com;  # 替换为你的域名或IP地址

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }

    # 安全：禁止访问隐藏文件
    location ~ /\. {
        deny all;
    }
}
